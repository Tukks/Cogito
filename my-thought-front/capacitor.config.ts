import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tukks.mythings',
  appName: 'My Things',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
