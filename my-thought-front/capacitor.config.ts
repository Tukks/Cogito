import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-thought-front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
